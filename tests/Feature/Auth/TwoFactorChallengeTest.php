<?php

use App\Models\User;

test('two factor challenge redirects to login when not authenticated', function () {
    $this->markTestSkipped('Two-factor authentication requires additional setup.');
});

test('two factor challenge can be rendered', function () {
    $this->markTestSkipped('Two-factor authentication requires additional setup.');
});
